services:
  app:
    build:
      context: .
      target: production
    restart: unless-stopped
    volumes:
      - storage:/var/www/html/storage
      - telegram_sessions:/var/www/html/telegram_sessions
      - public_build:/var/www/html/public/build
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    environment:
      DB_HOST: postgres
      HANDLER_PHP_BINARY: /usr/local/bin/php

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./public:/var/www/html/public:ro
      - public_build:/var/www/html/public/build:ro
      - storage:/var/www/html/storage:ro
    depends_on:
      - app

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/watcher_backup.sql:/docker-entrypoint-initdb.d/01-restore.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 10

  queue:
    build:
      context: .
      target: production
    restart: unless-stopped
    command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
    healthcheck:
      disable: true
    volumes:
      - storage:/var/www/html/storage
      - telegram_sessions:/var/www/html/telegram_sessions
    depends_on:
      postgres:
        condition: service_healthy
    env_file: .env
    environment:
      DB_HOST: postgres
      HANDLER_PHP_BINARY: /usr/local/bin/php

volumes:
  postgres_data:
  storage:
  telegram_sessions:
  public_build:
